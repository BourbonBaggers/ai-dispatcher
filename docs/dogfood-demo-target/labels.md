# Demo intake labels

Use these labels on the demo issue:

- `dispatch:ready` to admit the issue into the dispatcher scan
- `agent:codex` to route the run to Codex
- `priority:p1` to keep the demo easy to find
- `effort:small` to keep the routing narrow and predictable

Do not enable autoship for the first dogfood pass.

